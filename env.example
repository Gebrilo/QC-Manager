# QC Management Tool - Environment Configuration
# Copy this file to .env and update the values before deploying
#
# For Local Development:
#   The docker-compose.override.yml provides dev defaults, so .env is optional locally
#
# For VPS Deployment:
#   1. Copy this file: cp env.example .env
#   2. Update all values marked with "CHANGE_THIS"
#   3. Generate secure passwords using: openssl rand -hex 16
#   4. Replace YOUR_VPS_IP with your actual server IP

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
POSTGRES_USER=qc_user
POSTGRES_PASSWORD=CHANGE_THIS_secure_password_here
POSTGRES_DB=qc_app

# Database connection string (update password to match POSTGRES_PASSWORD)
DATABASE_URL=postgresql://qc_user:CHANGE_THIS_secure_password_here@postgres:5432/qc_app

# =============================================================================
# API CONFIGURATION
# =============================================================================
# JWT Secret - MUST be changed in production (min 32 characters)
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_THIS_generate_random_64_char_string_here

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# For VPS: Use your server's IP or domain
# Example: http://192.168.1.100/api or https://yourdomain.com/api
NEXT_PUBLIC_API_URL=http://YOUR_VPS_IP/api

# =============================================================================
# N8N WORKFLOW AUTOMATION
# =============================================================================
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=CHANGE_THIS_n8n_password
N8N_HOST=YOUR_VPS_IP
N8N_PROTOCOL=http
WEBHOOK_URL=http://YOUR_VPS_IP:5678

# =============================================================================
# OPTIONAL: SSL/HTTPS (uncomment if using SSL)
# =============================================================================
# N8N_PROTOCOL=https
# WEBHOOK_URL=https://YOUR_DOMAIN:5678
