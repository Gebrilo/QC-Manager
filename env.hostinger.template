# QC Management Tool - Hostinger Environment Configuration
# Copy these values to Hostinger Docker Manager environment variables

# PostgreSQL Database
POSTGRES_USER=qc_user
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD
POSTGRES_DB=qc_app
DATABASE_URL=postgresql://qc_user:CHANGE_THIS_SECURE_PASSWORD@postgres:5432/qc_app

# API Server
JWT_SECRET=GENERATE_RANDOM_SECRET_HERE

# Frontend - IMPORTANT: Replace YOUR_VPS_IP with your actual Hostinger VPS IP
NEXT_PUBLIC_API_URL=http://YOUR_VPS_IP/api

# n8n Automation
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=CHANGE_N8N_PASSWORD
N8N_HOST=YOUR_VPS_IP
WEBHOOK_URL=http://YOUR_VPS_IP:5678

# Instructions:
# 1. Generate secure passwords:
#    - PostgreSQL: openssl rand -hex 16
#    - JWT Secret: openssl rand -hex 32
#    - n8n Password: openssl rand -hex 12
#
# 2. Replace ALL occurrences of YOUR_VPS_IP with your actual IP
#
# 3. In Hostinger Docker Manager:
#    - Go to your application
#    - Add each variable above (one per line)
#    - Format: KEY=value (no quotes needed)
