{
  "name": "Task Created Handler",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "task-created",
        "options": {}
      },
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [250, 300]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO audit_logs (entity_type, entity_id, action, payload, created_at) VALUES ('task', '{{$json.body.id}}', 'TASK_CREATED', '{{JSON.stringify($json.body)}}', NOW())"
      },
      "name": "Log Task Created",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 1,
      "position": [450, 300],
      "credentials": {
        "postgres": {
          "id": "1",
          "name": "QC Tool Postgres"
        }
      }
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Log Task Created",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
