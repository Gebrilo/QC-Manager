{
  "name": "Project Created Handler (WF-001)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "project-created",
        "options": {}
      },
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [250, 300]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO audit_logs (entity_type, entity_id, action, payload, created_at) VALUES ('project', '{{$json.body.id}}', 'PROJECT_CREATED', '{{JSON.stringify($json.body)}}', NOW())"
      },
      "name": "Log Project Created",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 1,
      "position": [450, 300],
      "credentials": {
        "postgres": {
          "id": "supabase-postgres-id",
          "name": "QC Supabase Postgres"
        }
      }
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Log Project Created",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
